#include<stdio.h>
int fact(int);
int main()
{
int num;
printf("enter positive number to find its factorial:");
scanf("%d",&num);
printf("factoriala of %d is",num,fact(num));
return 0;
}
int fact(int num)
{
if(num)
return(num*fact(num-1));
else
return 0;
}
